#include<iostream>
using namespace std;

int main () {
    int number;

    cout<<"Enter the number = ";
    cin>>number;

    if (number >= 1 && number <= 10) {
        cout<<"Yes";
    }
    else {
        cout<<"No";
}
}