// Area of Triangle
const prompt = require('prompt-sync')();
let  base = Number(prompt("Enter an baase : "));
let  height = Number(prompt("Enter an height : "));


Area =(base*height)/2;

console.log(Area)