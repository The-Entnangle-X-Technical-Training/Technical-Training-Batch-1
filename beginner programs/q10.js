// Write a program that calculates the area of a rectangle by taking length and width as input.



const prompt = require('prompt-sync')();
let  base = Number(prompt("Enter a  base no: "));
let  hight = Number(prompt("Enter a  hight: "));
area = base*hight/2
console.log(area)