#include <iostream>
using namespace std;
int main()
{
    int a, b;
    cout << "write the number a:" << endl;
    cin >> a;
    b = a / 10;
    cout << "the last digit is being remove the rest is shown  :" << b << endl;

    return 0;
}
